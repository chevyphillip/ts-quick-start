{
  "name": "ts-quick-start",
  "version": "1.0.0",
  "description": "Typescript up and running",
  "main": "index.js",
  "author": "Chevy Phillip",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist/* && rm -rf node_modules",
    "rebuild": "yarn clean && yarn install && yarn build",
    "build": "yarn tsc",
    "start": "nodemon",
    "watch": "yarn tsc --watch",
    "test": "jest"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1"
  }
}
